{
  "name": "uyim-mobile",
  "version": "0.1.0",
  "private": true,
  "description": "Uyim.uz — React Native (Expo) mobile app skeleton. See ../docs/MOBILE_APP_PLAN.md before continuing: run `npx create-expo-app` in this directory (or a fresh one) and carry over src/theme, src/api and src/store as-is — they're framework-agnostic TypeScript with no Expo-specific dependencies.",
  "scripts": {
    "start": "expo start",
    "android": "expo start --android",
    "ios": "expo start --ios"
  },
  "dependencies": {
    "expo": "~52.0.0",
    "expo-router": "~4.0.0",
    "expo-secure-store": "~14.0.0",
    "react": "18.3.1",
    "react-native": "0.76.5",
    "@tanstack/react-query": "^5.62.0",
    "zustand": "^5.0.2"
  },
  "devDependencies": {
    "typescript": "^5.6.0",
    "@types/react": "~18.3.12"
  }
}
